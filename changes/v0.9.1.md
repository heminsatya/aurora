# Changes to Aurora Framework v0.9.1 beta

- Added `replace_file_lines` method for replacing multiple lines in a file between two strings
- Fixed `init-db` method bug for CLI class.
- Improved CLI commands.
